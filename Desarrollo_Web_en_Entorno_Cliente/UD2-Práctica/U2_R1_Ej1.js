/* Realiza un programa JS que calcule el área y la longitud de una circuferencia de radio 5*/

var area = 0.0;
var longitud = 0.0;
var radio = 5.0;

// Calcular el área de la circunferencia 
area = Math.PI * Math.pow(radio, 2);

// Calcular la longitud de la circunferencia
longitud = 2 * Math.PI * radio;

// mostramos en consola
console.log(area);
console.log(area);

    